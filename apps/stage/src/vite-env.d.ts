/// <reference types="vite/client" />

/**
 * The on-screen version chip's source (`src/panels/TitleStrip.tsx`), injected by `vite.config.ts`
 * from `git describe --tags --always --dirty` at build time, or the literal `'dev'` in the dev
 * server. See that file's `define` block for how the string is produced.
 */
declare const __STAGE_VERSION__: string;
